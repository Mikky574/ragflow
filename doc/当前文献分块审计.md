# 当前文献分块审计

审计对象：`3686160923High-efficiency_doherty_amplifier_usin.pdf`
审计日期：2026-09-19

## 当前配置与基线对比

| 基线项 | 当前值 | 结果 | 说明 |
| --- | --- | --- | --- |
| 解析器 | Paper | 通过 | 适合保留论文版面和位置。 |
| 单块上限 | 800 token | 未通过 | 基线为 500–600 token。 |
| 重叠 | 5.1% | 通过 | 位于 5–10% 范围。 |
| 父子分块 | 开启 | 未通过 | 基线先关闭，待独立块质量通过后再启用。 |
| 表格上下文 | 1 | 通过 | 符合基线。 |
| 图片上下文 | 1 | 未通过 | 基线为 0，避免图内 OCR 噪声扩散。 |

配置基线满足 **3 / 6** 项。

## 当前索引结果

- 可检索原始块：23。
- 母块：13。
- 页面记录总数：36。
- 带母块关联的子块：13。
- 每个母块都只关联 1 个子块。
- 这 13 个子块与各自母块的文本完全相同。

结论：当前父子分块没有产生更细的子块，只生成了 13 对重复记录。这解释了页面中的重复内容，也没有带来“小块检索、大块回答”的收益。

## 内容自检结论

| 检查项 | 状态 | 依据 |
| --- | --- | --- |
| 标题与作者信息覆盖 | 通过 | 首块保留标题、单位、地址和邮箱。 |
| 主要正文覆盖 | 基本通过 | 引言、方法、实验和结论均有文本块。 |
| 图注覆盖 | 基本通过 | `Fig. 2. Full schematic ...` 可见于图相关块。 |
| 图文关联 | 未通过 | 正文引用、图注和图内 OCR 常被拆为独立块。 |
| 双栏阅读顺序 | 有风险 | 部分图表页出现正文与图内文字混排。 |
| 图内 OCR 噪声控制 | 未通过 | 若干块主要由坐标轴、图例和扫描参数组成。 |
| 块大小与主题聚焦 | 有风险 | 既有很短的图内 OCR 块，也有 2,692 字符的正文块。 |
| 父子块有效性 | 未通过 | 父子关联全部为一对一重复。 |

## 已完成的代码调整

1. Paper 解析器在启用父子分块时，用空行保留相邻版面文本块的段落边界，供子块分隔符使用。
2. 只有一个母块实际拆出多个子块时，才保留母块；未拆开的块不再写入重复母块。

该结论是重新解析前的审计基线；下方记录了基线配置重新解析后的实际结果。

## 下一轮建议

先以基线配置重新解析并审阅：`Paper`、550 token、8% 重叠、关闭父子分块、表格上下文 1、图片上下文 0。若正文检索仍经常缺少前后条件，再启用父子分块验证新的边界逻辑。

## 基线实验结果（2026-09-19）

已按 PDF 论文处理方案重新解析同一篇文献，配置为：Paper、550 token、8% 重叠、关闭父子分块、表格上下文 1、图片上下文 0。

| 指标 | 原父子分块配置 | 基线实验 | 结论 |
| --- | ---: | ---: | --- |
| 可检索原始块 | 23 | 25 | 正文上限降低后，正文边界更细。 |
| 母块 | 13 | 0 | 重复母子记录已消除。 |
| 页面总记录数 | 36 | 25 | 不再把一对一母子副本显示为额外分块。 |
| 母子完全重复记录 | 13 | 0 | 父子分块重复问题已解决。 |
| 总 token | 5,398 | 5,290 | 内容没有因重新解析明显丢失。 |
| 文本块中位长度 | 407 字符 | 416 字符 | 主体粒度基本稳定。 |
| 最大文本块 | 2,692 字符 | 2,185 字符 | 过长块有所收敛，仍需继续审阅。 |

本轮通过：配置基线全部生效、重复母子记录消失、正文最大块变小。

本轮仍未通过：图内 OCR 碎块仍存在，图号、图注和正文引用仍未建立可靠关联，双栏图表页的阅读顺序仍需在真实检索问题中核验。下一步不再通过降低 token 解决这些问题，应按 PDF 论文处理方案实施内容角色标记、噪声降权和图号关联。

## 图表检索文本过滤实验（2026-09-19）

实现图片块的图注优先策略：只将含唯一图号的图注写入图片块的可检索文本；图内 OCR、多个图号混合的错误关联和没有可靠图注的图片不生成可检索图片块。原 PDF 与图片位置仍可从文档预览溯源。

| 指标 | 过滤前 | 过滤后 | 结论 |
| --- | ---: | ---: | --- |
| 总检索块 | 25 | 20 | 5 个没有可靠语义的图片区块被移除。 |
| 图片检索块 | 9 | 4 | 纯图内 OCR 与错误关联块不再参与召回。 |
| 表格检索块 | 1 | 1 | 表格仍保留。 |
| 总 token | 5,290 | 4,271 | 移除的主要是图内 OCR 噪声。 |
| 母块 | 0 | 0 | 基线阶段仍不启用父子分块。 |

保留下来的图片块均含图号，例如 `Fig. 2. Full schematic of the class-F PA for high efficiency.`。仍需处理的事项：部分图注只保留了首行；Table I 的标题中混入 Fig. 9 文本；正文中的图号引用尚未与图注建立 `related_chunk_ids`。这些属于下一步的图号关联与表格标题清理，不应通过重新放开图内 OCR 来解决。

## revision 3 版面与图表清理复核（2026-09-19）

本轮以同一篇论文重新入库。Paper 解析器配置保持为：`chunk_token_num=550`、重叠 `8%`、表格上下文 `1`、图片上下文 `0`，父子块关闭。

| 检查项 | 结果 | 索引证据 |
| --- | --- | --- |
| 列数识别 | 通过 | 4 个页面均选中 2 列；全局列数为 2。 |
| 图内 OCR 噪声 | 通过 | 索引中只保留 4 个图块，均有 `Fig.` 标识；无坐标轴或图例专用块。 |
| Table I 与 Fig. 9 混入 | 通过 | 表格 caption 为 `TABLE I`，不再含 `Fig. 9`。 |
| 表格页码残片 | 未通过 | 表格检索文本仍以 `77-80.` 开头，需要在表格写入索引前剔除孤立页码。 |
| 父子重复 | 通过（基线） | 20 个索引块，`mom_id` 均为空；当前无母子副本。 |
| 块大小上限 | 未通过 | 15 个正文块的索引词元范围为 162–1905；其中 8 个超过 550。 |
| 双栏阅读顺序 | 待人工语义抽查 | 列数已修正，但尚未逐句验证左右栏是否严格按阅读顺序连接。 |
| 真实检索排名 | 未验证 | 尚未执行目标、方法、参数、结论和 `Fig. 2` 的 Top-3/5 查询集。 |

最新结果为 **20 个可检索块、4204 token、0 个母块**。下一段代码工作应先按章节/段落边界拆分超长正文块，并清理表格前置页码；图号关联和父子块在这些边界稳定后再启用。

## revision 6 最终索引复核（2026-09-19）

已重启 Python worker 后重新解析，确认新代码已加载。结果为 **20 个可检索块、4200 token、0 个母块**；Table I 由 968 字符降至 961 字符，前置 `77-80.` 已移除。正文配置 token 范围为 60–549，满足 550 的上限；索引词条数较大是检索分词粒度，不能与配置 token 混用。

## 当前失败项的实现优先级（revision 6）

1. **摘要与正文续句边界**：这是当前最直接影响检索回答的问题。摘要需要独立收集到下一主章节；普通正文新块需要上一块末尾的句级重叠和章节标题。
2. **出版商页脚清理**：只删除可识别的 IEEE 授权/下载/限制组合，不删除 Acknowledgment 正文。
3. **图注和图号关系**：caption 版面文本优先；正文和图块均携带图号元数据，检索层据图号做关联扩展。
4. **顺序与检索验收**：列数识别已经通过，下一步是两页阅读顺序回归和固定问题集的 Top-5 验证。

当前不建议通过提高 `chunk_token_num`、重新打开图内 OCR 或立即启用父子块来解决这些问题：它们无法修复摘要续块、页脚和图号关系，且会掩盖边界质量。

## revision 8 真实文献复测（2026-09-19）

Python worker 重启后使用同一篇论文重新入库。结果为 **20 个检索块、4167 token、0 个母块**。摘要独立为 309 token；IEEE 授权页脚已删除；Acknowledgment、References 与 Conclusions 均已分开。正文最大为 516 token，满足 550 token 上限。

剩余质量问题集中在少量 OCR 原始版面块的句中截断，以及尚未进行的双栏逐句和 Top-5 检索验收。图号关联仍需作为下一轮独立实现。
